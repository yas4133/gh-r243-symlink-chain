# GH-R243 owned two-principal Actions laboratory

Base workflow accepts only data artifact from external pull request. Privileged consumer extracts it
to isolated directory containing trusted symlink, then runs base-branch script outside destination.
